package behaviours.mate;

import jade.core.Agent;
import jade.core.behaviours.TickerBehaviour;

public class MateManager extends TickerBehaviour {
    public MateManager(Agent a, long period) {
        super(a, period);
    }

    @Override
    protected void onTick() {

    }
}
